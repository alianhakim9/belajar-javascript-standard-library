// string instance method & properties
const nama = "Alian Hakim";

console.info(nama.length);
console.info(nama.toLowerCase());
console.info(nama.toUpperCase());
console.info(nama.split(" "));

const input = " input data ";
console.info(input.trim()); // menghapus kiri kanan spasi
console.info(input.trimEnd());
console.info(input.trimStart());
